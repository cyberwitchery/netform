[package]
name = "netform_cli"
version = "0.10.0"
edition = "2024"
license = "MIT"
description = "CLI for diffing lossless network configuration IR documents"
repository = "https://github.com/cyberwitchery/netform"
homepage = "https://github.com/cyberwitchery/netform"
documentation = "https://docs.rs/netform_cli"

[[bin]]
name = "config-diff"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
netform_dialects = { version = "0.10.0", path = "../netform_dialects" }
netform_diff = { version = "0.10.0", path = "../netform_diff" }
netform_ir = { version = "0.10.0", path = "../netform_ir" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
